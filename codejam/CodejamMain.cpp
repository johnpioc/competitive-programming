#include <iostream>
#include <map>
#include <queue>

using namespace std;

int main() {
    // Take in region size, patient zero location

    // Take in map string and store it as a vector of char vectors
}